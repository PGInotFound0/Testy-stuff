# Runtime settings are intentionally not compiled from environment variables.
# Copy public/config.example.json to public/config.json and set homeserverUrl.
# At deployment time, serve /config.json from the same origin as the client.
